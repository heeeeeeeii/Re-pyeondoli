<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<!-- namespace : 자바클래스에서 mapper 를 찾는 경로 -->
	<mapper namespace="py.mappers.BranchOfficeMapper">
	
	<!-- 지점 목록 -->
	<select id="selectList" resultType="vo.BotimeVO"> 
		select brname, brtime from botime order by brname
	</select>
</mapper>