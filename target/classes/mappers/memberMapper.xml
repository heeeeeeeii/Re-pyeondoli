<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<!-- namespace : 자바클래스에서 mapper 를 찾는 경로 -->
	<mapper namespace="py.mappers.memberMapper">
	
	<!-- 회원 목록 -->
	<select id="selectList" resultType="vo.MemberVO"> 
		select id, pw, nickname, email, memimg, point from member order by id
	</select>

	<!-- 로그인 -->
	<select id="selectOne" resultType="vo.MemberVO">
		select * from member where id=#{id}
	</select>
	
	<!-- 회원가입 -->
	<insert id="insert" parameterType="vo.MemberVO">>
		insert into member
		(id, pw, nickname, email, memimg, point) 
		VALUES
		(#{id}, #{pw}, #{nickname}, #{email}, #{memimg}, DEFAULT)
	</insert>
	
	<!-- 아이디 중복확인 -->
	<select id="IdCheck" parameterType="String" resultType="int">
		select count(*) from member where id=#{id}
	</select>
	
	<!-- 이메일 중복확인 -->
	<select id="EmailCheck" parameterType="String" resultType="int">
		select count(*) from member where email=#{email}
	</select>
	
	<!-- 아이디 찾기  -->
	<select id="findid" resultType="vo.MemberVO">
		select * from member where email=#{email}
	</select>
	
	<!-- 비밀번호 변경 -->
	<update id="pwupdate">
		update member set pw=#{pw} where id=#{id}
	</update>
	
	<!-- 닉네임 변경 -->
	<update id="nicknameupdate">
		update member set nickname=#{nickname} where id=#{id}
	</update>
	
	<!-- 이메일 변경 -->
	<update id="emailupdate">
		update member set email=#{email} where id=#{id}
	</update>
	
	<!-- 이미지 변경 -->
	<update id="memimgupdate">
		update member set memimg=#{memimg} where id=#{id}
	</update>
	
	<!-- 회원탈퇴 -->
	<select id="delete">
		delete from member where id=#{id}
	</select>
</mapper>